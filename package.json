{
  "name": "cy-10",
  "version": "1.0.0",
  "description": "Cypress Cucumber demo for cypress v10+",
  "main": "index.js",
  "scripts": {
    "test": "npx mocha test/**/*.spec.js",
    "cy": "npx cypress run --browser chrome --e2e",
    "cyo": "npx cypress open --browser chrome --e2e"
  },
  "keywords": [],
  "author": "Dan rusu",
  "license": "MIT",
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "20.0.0",
    "@cypress/browserify-preprocessor": "3.0.2",
    "cypress": "13.6.1",
    "gherkin-parse": "^1.0.8",
    "glob": "^10.3.10"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": [
      "cypress/e2e/step_definitions/**/*.{js,ts}"
    ]
  },
  "devDependencies": {
    "chai": "^4.3.10",
    "mocha": "^10.2.0"
  }
}
